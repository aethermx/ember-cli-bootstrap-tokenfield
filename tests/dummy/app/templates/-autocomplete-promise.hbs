<div class="panel panel-info">
  <div class="panel-heading">
    Autocomplete with a promise
  </div>

  <div class="panel-body">
    <p>Promises can be used to update the autocomplete object used by jQuery UI.</p>
    {{input-tokenfield value="banana,apple,watermelon" 
                       placeholder="Type something and hit enter"
                       autocomplete=autocompletePromise
                       showAutocompleteOnFocus=true}}
    <br>

    Controller
    <pre>// contribed example, this data is likely to come from ember-data
autocompletePromise: Ember.computed({
  var autocomplete = {
    source: ['banana','apple','watermelon','pear','grape','peach','mango'],
    delay: 100
  };

  return new Ember.RSVP.Promise(function(resolve) {
    Ember.run.later(function() {
      resolve( autocomplete );
    }, 3000); // simulate a 3 seconds delay 
  });
})</pre>
    Template
<pre>\{{input-tokenfield value="banana,apple,watermelon" 
                   placeholder="Type something and hit enter"
                   autocomplete=autocompletePromise
                   showAutocompleteOnFocus=true}}</pre>
  </div>
</div>
